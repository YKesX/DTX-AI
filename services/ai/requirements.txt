# DTX-AI runtime + training dependencies.
#
# Versions pinned to exactly what the committed model artifacts
# (services/ai/ai/models/**/*.pkl, *.pth) were trained with — DO NOT bump
# without retraining via scripts/train_models.py, otherwise sklearn will emit
# InconsistentVersionWarning at load time and behaviour may drift silently.

# Core data + scientific stack
numpy==2.4.4
pandas==3.0.2

# Tree models + scaler
scikit-learn==1.8.0
lightgbm==4.6.0
xgboost==3.2.0
joblib==1.5.3

# LSTM-AE (PyTorch). The CUDA build must be installed from the official
# pytorch index for GPU support — see docs/ml_training.md.
# torch==2.11.0  (install via: pip install torch --index-url https://download.pytorch.org/whl/cu128)
pytorch-tabnet>=4.1.0

# XAI
shap>=0.45

# Dataset acquisition (notebook only)
kagglehub>=0.2

# Plotting (notebook + training script reports)
matplotlib==3.10.9

# macOS runtime note:
# LightGBM/XGBoost wheels require OpenMP (libomp.dylib). Install libomp via
# your system package manager before strict-replay use of these models.
