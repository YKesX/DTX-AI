# API service runtime + test dependencies. Pinned to versions verified in the
# local .venv — bump deliberately and retest /events round-trip after.
fastapi==0.136.1
uvicorn[standard]==0.46.0
python-dotenv==1.2.2
pydantic==2.13.3
aiosqlite==0.22.1

# Test dependencies (kept here so a single `pip install -r` covers
# `pytest tests/integration/`).
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.3.0
