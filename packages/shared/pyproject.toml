[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dtx-ai-shared"
version = "0.1.0"
description = "Shared schemas for DTX-AI"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.6",
]
